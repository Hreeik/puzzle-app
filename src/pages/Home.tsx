import { FC } from 'react';
import './Home.css';

const Home: FC = () => {
    return (
        <div className="home-container">
            <p>Click "Play" in the menu to start the game.</p>
        </div>
    );
};

export default Home;
